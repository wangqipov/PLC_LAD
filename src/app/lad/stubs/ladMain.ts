/**
 * Production uses lad/virtualScrolling/ladMain.
 * Demo/Next rebuilds the canvas via updateCanvas inside ForwardOrBackFun.
 */
export function triggerUndoReDo(_programData: unknown): void {
    /* no-op outside vscode */
}
