/**
 * Ladder-diagram geometry constants. transformData.setFB uses these for FB width / pin height.
 */
const config = {
    FBpinHeight: 0.6,
    fBMargin: 1,
    fontWidth: 0.5,
    pinLength: 0.25,
    basicLength: 30,
};

export default config;
